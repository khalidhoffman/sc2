doctype html
html
    head
        meta(name="viewport", content="width=device-width, initial-scale=1")
        title= title
        link(rel='stylesheet', href='/stylesheets/styles.css')
    body
        #root
        script(src='./js/dist/sc2.vendor.js', type='text/javascript')
        script(src='./js/dist/sc2.app.js', type='text/javascript')
        script.
            if (location.href.match(/live/)) {
                var body = document.getElementsByTagName('body')[0];
                var livejsScript = document.createElement("script");
                Object.assign(livejsScript, {
                    src: './js/live.js',
                    type: 'text/javascript'
                })
                body.appendChild(livejsScript);
            }
